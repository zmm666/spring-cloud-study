Hystrix === 故障转移，服务降级
    服务降级 ： 未开启熔断，调用服务异常，执行降级方法，然后接着执行主逻辑
    熔断器 ： 开启熔断，调用服务异常，达到设置的参数阀值，则直接执行降级方法，待休眠期窗口过期，则半开启熔断器，尝试调用服务，
        异常则初始化休眠期窗口，正常则恢复，关闭熔断器。

网关Zuul === 微服务统一入口，路由转发（可自定义路由规则），配合ribbon负载均衡， 过滤器